# Umcomment this line in the file ".env.production.local"
# NODE_ENV=production

# https://node-postgres.com/features/connecting#environment-variables
PGHOST=
PGPORT=
PGUSER=
PGPASSWORD=
PGDATABASE=

POSTGRESQL_CONNECTION_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}